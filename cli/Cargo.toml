[package]
name = "wmux-cli"
version = "0.1.0"
description = "wmux command line interface (named pipe client)"
edition = "2021"

[[bin]]
name = "wmux"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
named_pipe = "0.4"